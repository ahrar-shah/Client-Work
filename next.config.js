/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow sending cookies & sameSite none if needed — for demo same-origin is fine.
}

module.exports = nextConfig
